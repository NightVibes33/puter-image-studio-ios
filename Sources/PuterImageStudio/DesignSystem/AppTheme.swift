import SwiftUI

enum AppTheme {
    static let pageBackground = Color(uiColor: .systemGroupedBackground)
    static let panelBackground = Color(uiColor: .secondarySystemGroupedBackground)
    static let elevatedBackground = Color(uiColor: .tertiarySystemGroupedBackground)
    static let ink = Color(uiColor: .label)
    static let secondaryInk = Color(uiColor: .secondaryLabel)
    static let accent = Color(red: 0.18, green: 0.47, blue: 0.91)
    static let warmAccent = Color(red: 0.96, green: 0.42, blue: 0.20)
    static let success = Color(red: 0.10, green: 0.58, blue: 0.34)

    static let cornerRadius: CGFloat = 8
    static let controlHeight: CGFloat = 48
}
